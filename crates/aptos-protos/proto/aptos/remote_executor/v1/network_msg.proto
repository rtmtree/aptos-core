// Copyright © Aptos Foundation
// SPDX-License-Identifier: Apache-2.0

syntax = "proto3";

package aptos.remote_executor.v1;

message NetworkMessage {
    bytes sender_addr = 1;
    bytes message = 2;
    string message_type = 3;
}

message Empty {

}

service NetworkService {
    rpc Send(NetworkMessage) returns (Empty);
}